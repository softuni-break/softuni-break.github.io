// import { e$ } from './elements.js';

// export const showProgress = (minutes, seconds, countdownTime) => {

//     seconds = Number(seconds);
//     minutes = Number(minutes);

//     let totalTime = seconds + (minutes * 60);

//     e$.time.innerLayer().style.width = `${(totalTime / (countdownTime)) * 100}%`;
// };